# MCP Server Configuration
# Copy this file to .env and fill in your values.

# --- Figma Integration ---
# Personal Access Token from Figma (Settings > Account > Personal access tokens)
FIGMA_ACCESS_TOKEN=figd_...

# Secret passcode configured in the Figma Webhook settings
# Used to verify that requests actually came from Figma
FIGMA_WEBHOOK_PASSCODE=my-secret-passcode

# --- GitHub Automation ---
# Personal Access Token (Classic or Fine-grained) with 'repo' scope
GITHUB_TOKEN=ghp_...

# Target repository for creating Pull Requests (format: owner/repo)
GITHUB_REPO=my-org/my-project-repo

# --- Security ---
# Secret key used to sign internal Approval Tokens for write operations
# Generate a random string: openssl rand -hex 32
MCP_APPROVAL_SECRET=change-this-to-a-secure-random-string

# --- Development Overrides ---
# Set to 'true' to bypass allowed_repos list in config (DEV ONLY)
# DEV_ALLOW_ALL_REPOS=false
